import { ChecklistValidator } from './checklist-validator';

describe('ChecklistValidator', () => {
  it('should create an instance', () => {
    expect(new ChecklistValidator()).toBeTruthy();
  });
});
