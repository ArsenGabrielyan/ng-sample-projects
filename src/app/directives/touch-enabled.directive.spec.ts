import { TouchEnabledDirective } from './touch-enabled.directive';

describe('TouchEnabledDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchEnabledDirective();
    expect(directive).toBeTruthy();
  });
});
