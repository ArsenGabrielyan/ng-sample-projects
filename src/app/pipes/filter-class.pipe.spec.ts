import { FilterClassPipe } from './filter-class.pipe';

describe('FilterClassPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClassPipe();
    expect(pipe).toBeTruthy();
  });
});
